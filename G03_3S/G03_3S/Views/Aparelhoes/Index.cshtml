@model IEnumerable<G03_3S.Aparelho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeAparelho)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.NomeUsuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeAparelho)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.NomeUsuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAparelho }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAparelho }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAparelho })
        </td>
    </tr>
}

</table>
